name: Hello_python

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Greetings
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
      python3 app/python/hello.py